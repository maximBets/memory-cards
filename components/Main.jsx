const React = require('react');
const Layout = require('./Layout');

function Main() {
  return (
    <Layout>
      <h1 className="main-title">Memory Cards</h1>
    </Layout>
  );
}

module.exports = Main;
